# Backend seams — all optional while the UI runs on mock data
VITE_API_BASE_URL=/api/v1
VITE_WS_URL=wss://cctv-gateway.gujpolice.local/realtime
VITE_STREAM_GATEWAY=https://cctv-gateway.gujpolice.local
